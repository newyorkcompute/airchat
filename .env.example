# Vercel AI Gateway key — create one at https://vercel.com/dashboard → AI Gateway → API Keys
AI_GATEWAY_API_KEY=

# Optional: override the model (provider/model gateway slug)
# AIRCHAT_MODEL=openai/gpt-5.4-mini
